#!groovy
@Library("Infrastructure") _

properties([
  parameters([
    string(name: 'ENVIRONMENT', defaultValue: 'saat', description: 'Environment where code should be build and deployed'),
  ]),
  [$class: 'GithubProjectProperty', projectUrlStr: 'https://github.com/hmcts/submit-your-appeal'],
  pipelineTriggers([[$class: 'GitHubPushTrigger']])
])

withParameterizedPipeline('nodejs', 'sscs-tribunals', 'frontend', params.ENVIRONMENT, 'sandbox') {

}
